// How many photo "slots" each category reserves. Not a data file per
// image — just a number, so adding capacity for more photos in a
// category is a one-line change, not a data-entry chore. See
// frontend/public/gallery/README.md for the exact filename convention
// each slot expects.
export const IMAGES_PER_CATEGORY = 6;

export const GALLERY_BASE_PATH = "/gallery";
